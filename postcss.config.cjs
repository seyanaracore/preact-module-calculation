module.exports = {
  plugins: [
    require('autoprefixer'),
    // require('postcss-autoreset')({
    //   rulesMatcher: (rule) => !rule.selector.includes('.dataTable'),
    //   reset: {
    //     margin: 0,
    //     padding: 0,
    //     borderRadius: 0,
    //   },
    // }),
  ],
}
