export type ModuleItem = {
  name: string;
  width: number;
  height: number;
  power: number;
  id: number;
  sku: string;
  price: number;
};

export type ControllerItem = { name: string; id: number; price: number };
